# Install alongside requirements.txt for the same-desktop client/server setup.
pyrealsense2>=2.55,<3
numpy>=1.26,<3
opencv-python>=4.8,<5
ultralytics>=8.3,<9
# ByteTrack's assignment solver; preinstall to avoid runtime auto-installation.
lap>=0.5,<1
